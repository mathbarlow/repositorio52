<%= form_with(model: calificaciones, local: true) do |form| %>
  <% if calificaciones.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(calificaciones.errors.count, "error") %> prohibited this calificaciones from being saved:</h2>

      <ul>
      <% calificaciones.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :puntaje %>
    <%= form.number_field :puntaje %>
  </div>

  <div class="field">
    <%= form.label :comentario %>
    <%= form.text_field :comentario %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
